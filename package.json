{
  "name": "spectruth",
  "version": "0.1.0",
  "private": true,
  "description": "Spec conformance verifier — independently checks if AI-generated code satisfies your Kiro specs",
  "author": "7vignesh",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
