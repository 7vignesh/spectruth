{
  "name": "spectruth-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo for SpecTruth — Done Integrity ship gate for Kiro spec tasks",
  "author": "7vignesh",
  "license": "MIT",
  "scripts": {
    "build": "pnpm --filter spectruth-core build && pnpm --filter spectruth build",
    "test": "pnpm --filter spectruth-core test",
    "demo": "node packages/cli/dist/index.js demo",
    "audit:example": "node packages/cli/dist/index.js audit --root examples/records-api",
    "clean": "pnpm -r clean"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
